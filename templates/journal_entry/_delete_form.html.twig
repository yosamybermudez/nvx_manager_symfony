<form method="post" action="{{ path('app_journal_entry_delete', {'id': journal_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ journal_entry.id) }}">
    <button class="btn">Delete</button>
</form>
